## 🔄 Ripple Auto-Fix: Breaking Change in `user.proto`

> This PR was automatically generated by [Ripple](https://ripple-cnn.pages.dev) — self-maintaining APIs that detect breaking changes, find consumers, and open fix PRs.

---

### ⚠️ Breaking Change Detected

| Field | Change | Source | Severity |
|-------|--------|--------|----------|
| `phone_number` | **removed** | `user.proto` | 🔴 Breaking |

The upstream contract `user.proto` removed the `phone_number` field from `CreateUserRequest`. All consumers passing this field will fail at compile time (strongly-typed languages) or silently send unused data (dynamic languages).

---

### 🤖 AI Confidence: 96%

| Signal | Score | Detail |
|--------|-------|--------|
| Observation frequency | 0.98 | Seen 14 times in commit history |
| Language confidence | 0.95 | Go — strongly typed, deterministic fix |
| Fix type | 0.97 | Template-based (field removal pattern) |
| Historical accuracy | 0.93 | Similar fixes merged without revert |

---

### 📊 Impact Report

- **Consumers affected:** 4
- **Repositories touched:** 3
- **Files changed:** 4
- **Lines modified:** 6 (3 removals, 3 edits)

| Service | File | Language | Change |
|---------|------|----------|--------|
| auth-service | `handler.go` | Go | Removed `PhoneNumber` field from `CreateUserRequest`, removed `phone` param |
| billing-api | `UserClient.ts` | TypeScript | Removed `phoneNumber` from `createUser()` payload |
| notifications | `user_svc.py` | Python | Removed `phone_number` kwarg from `create_user()` call |
| tests | `test_user_api.java` | Java | Removed `setPhoneNumber()` from test builder |

---

### 🔗 Upstream Status

| | |
|---|---|
| **Source CR** | `user-service#847` — *Remove deprecated phone_number field* |
| **Status** | ✅ **MERGED** |
| **Merged by** | @platform-team |
| **Merged at** | 2026-08-15 07:30 UTC |

---

### ✅ Fix Applied

- Removed `phone` parameter from `RegisterUser` function signature
- Removed `PhoneNumber: phone` from `userpb.CreateUserRequest` struct literal
- Removed `phone` from log format string
- All existing tests pass (no references to removed field)
- No behavioral change — phone was write-only (never read downstream)

---

### 🧪 Validation

```
✓ go build ./...          — compiles cleanly
✓ go vet ./...            — no issues
✓ go test ./...           — 23/23 passed
✓ staticcheck ./...       — clean
```

---

<sub>
📐 Powered by <a href="https://ripple-cnn.pages.dev">Ripple</a> — tested on <b>882 real changes</b> across 50 OSS repos via <a href="https://www.researchgate.net/publication/412220063">PropBench</a>.<br>
🧠 Learning enabled — this fix improves future accuracy for <code>field_removal</code> patterns in Go.<br>
🏷️ <code>ripple/auto-fix</code> · <code>breaking-change</code> · <code>proto</code>
</sub>
